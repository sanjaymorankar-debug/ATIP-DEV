# ATIP — AI Trading Intelligence Platform
# Install: pip install -r requirements.txt

# ── Core (Required) ─────────────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
requests>=2.31.0

# ── Dhan API (Primary data source — Indian stocks real-time + historical)
dhanhq>=1.4.0

# ── Global markets data (yfinance — US/Asia/Europe/Commodities/FX) ──────
yfinance>=0.2.38

# ── Technical indicators ─────────────────────────────────────────────────
# `ta` is the pure-Python implementation and the one this project is verified
# against (Python 3.14 / pandas 3.0 / numpy 2.5). It is REQUIRED — without a
# TA library, compute_indicators() returns nothing and the whole scoring
# engine silently produces empty technical inputs.
ta>=0.11.0
#
# pandas-ta is deliberately NOT pinned here. It cannot install or import on
# this stack for two independent reasons:
#   1. it does `from numpy import NaN`, removed in numpy 2.0 (you have 2.5.x);
#   2. it pulls in numba, which has no Python 3.14 wheel.
# technical.py still prefers pandas_ta when it imports cleanly (older stacks),
# and falls back to `ta` automatically — so installing it is optional and only
# worth trying on Python <= 3.12 with numpy < 2.
# pandas-ta>=0.3.14b0

# ── Scheduling ───────────────────────────────────────────────────────────
schedule>=1.2.0

# ── Web dashboard ────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# ── AI news classification ───────────────────────────────────────────────
anthropic>=0.25.0

# ── News RSS + web scraping ──────────────────────────────────────────────
feedparser>=6.0.11
beautifulsoup4>=4.12.0

# ── Zerodha (Optional — only if using Zerodha portfolio) ─────────────────
kiteconnect>=4.2.0

# ── Utilities ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
openpyxl>=3.1.0
